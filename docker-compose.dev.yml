services:
  web:
    build:
      context: .
    image: siib-website-dev-img
    container_name: siib-website-dev
    ports:
      - '3001:3000'
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
