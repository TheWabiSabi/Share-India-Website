services:
  web:
    build:
      context: .
    image: nextjs-app
    container_name: nextjs-app
    ports:
      - '3001:3000'
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
